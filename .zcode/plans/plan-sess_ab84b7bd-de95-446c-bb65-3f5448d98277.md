# Plan: Bring README.md and AGENTS.md up to date with the code

## Scope & guardrails
- **Files modified (3):** root `README.md`, root `AGENTS.md`, `docs/environment-variables.md`.
- **NOT touched:** anything under `image/` (including `image/AGENTS.md` / `image/.opencode/AGENTS.md` — the docker image's own file), no code/config/compose changes. This is a documentation-accuracy pass only.
- Every corrected value is verified against source code (citations below); nothing is guessed.

## Decision choices (best judgment, both reversible)
- **Env vars → single source of truth in `docs/environment-variables.md`**; README's inline table becomes a short summary + link. Eliminates the dual-table drift that caused this (README `qwen3.6-plus` vs code `glm-5`).
- **Dead links → inlined essentials, links removed/repurposed**; README stays self-contained.

---

## Phase 1 — `README.md` corrections

| # | Location | Problem (verified) | Fix |
|---|----------|--------------------|-----|
| 1 | `:119` dispatch sentence | Lists only `orchestration:*` + `implementation:ready/complete`; omits the `gh-issue-tracking:*` prefix namespace and the `gh-issue-tracking:direct-body` gate (`filters.py:54-55,61-62,73,125-137`) | Rewrite to: dispatch on `issues.labeled` from a non-bot actor for labels in `orchestration:*`, `gh-issue-tracking:*`, `implementation:ready`, `implementation:complete`. Note `gh-issue-tracking:direct-body` runs the issue body verbatim as the prompt and is fail-closed-gated by `DIRECT_BODY_ALLOWED_SENDERS`. |
| 2 | `:129` env table | `OPENCODE_MODEL` default `bailian-payg/qwen3.6-plus` | → `zai-coding-plan/glm-5` (`config.py:128`, `prompt.ps1:13`, `image/.opencode/opencode.json:8`) |
| 3 | `:164` dashboard | "the dashboard has no authentication" | Rewrite to token-gated: disabled (404) when `DASHBOARD_TOKEN` unset; requires a token (Bearer header / `?token=` / cookie, constant-time) and returns 401 on missing/wrong (`auth.py:11-21,44-53`; `dashboard.py:256-272`). |
| 4 | `:143-152` simulator section | Silent on token requirement | Add: even with `WEBHOOK_ENABLE_SIMULATOR=1`, `/simulator` returns **401** until `DASHBOARD_TOKEN` is set (`simulator.py:41-49`); token accepted via same 3 channels. |
| 5 | `:123-139` env-var table | Missing `OPENCODE_VARIANT`, `DASHBOARD_TOKEN`, `DIRECT_BODY_ALLOWED_SENDERS`, all watchdog vars, `TRACE_BLACKLIST_PATTERNS`; duplicates the env doc | Replace with a brief summary + link to `docs/environment-variables.md` (single source). Move the complete list (with correct defaults) into the env doc in Phase 3. |
| 6 | `:15,19,136` BeadsLoop | Only mentions `br ready --json` | State primary selector is `bvr --robot-next` (graph-aware) with `br ready --json` as fallback (`beads_loop.py:248-266,306-309`). |
| 7 | `:69,:74,:200` broken links | `docs/github-app-webhook-setup.md` doesn't exist (3 links); `plan_docs/archive/*` doesn't exist (real files under `.archived/`/`.deferred/`) | Inline the Tailscale Funnel + compose-overlay essentials into README; drop the dead links. Repoint the "Legacy / outdated docs" section to the real `plan_docs/.deferred/maestro_supervisor/` paths (and the archived webhook-setup doc under `plan_docs/.archived/agent-loop-refactor/`). |

## Phase 2 — `AGENTS.md` corrections

| # | Location | Problem | Fix |
|---|----------|---------|-----|
| 8 | `:237` Notes line | "Default model in `prompt.ps1` is `zai-coding-plan/glm-4.7`" | → `zai-coding-plan/glm-5` (`prompt.ps1:13`). Also note `--variant`/`--auto` flags. |
| 9 | `:21` config source-of-truth | Claims `image/opencode.json` and `image/AGENTS.md` (imprecise — top-level `image/` holds only `local_ai_instruction_modules/`) | Correct to `image/.opencode/opencode.json` and `image/.opencode/AGENTS.md` (the docker image's AGENTS.md, not the root one). Leave line :23 (already correct). |
| 10 | `:219-237` Scripts table | Omits 7 existing scripts | Add rows for: `dc.ps1`, `export-openapi.py`, `git-trust.sh`, `init-project-workspace.ps1`, `link-github-tracker.ps1`, `sync-agent-instruction-indices.ps1`, `webhook-entrypoint.sh` (descriptions verified from each script's synopsis). |

## Phase 3 — `docs/environment-variables.md` (make authoritative)

Add/expand so the complete, correct set lives in one place:
- Add `OPENCODE_MODEL` (default `zai-coding-plan/glm-5`) and `OPENCODE_VARIANT` (default `high`) — currently absent.
- Fix `OPENCODE_SERVER_LOG_PATH` default `/var/log/opencode-server/opencode.log` → `/home/app/.local/share/opencode/log/opencode.log` (`config.py:171`).
- Expand the grouped watchdog cell into individual rows with defaults: `IDLE_TIMEOUT_SECS` (900), `ERROR_GRACE_SECS` (300), `HARD_CEILING_SECS` (5400, falls back to `DISPATCH_TIMEOUT_SECS`), `WATCHDOG_POLL_SECS` (30), `MAX_CONSECUTIVE_ERRORS` (5), `WATCHDOG_DEBUG` (off), `PERMISSION_ASK_GRACE_SECS` (60).
- Add `TRACE_BLACKLIST_PATTERNS` (empty → built-in defaults; `filters.py:28-34`).
- Verify `DASHBOARD_TOKEN` and `DIRECT_BODY_ALLOWED_SENDERS` rows are accurate.

## Phase 4 — Validation
- Re-read every changed value against its source line to confirm accuracy.
- Manually confirm all markdown links/references resolve to files that actually exist (no remaining dead `docs/` or `plan_docs/archive/` links).
- Run `pwsh -NoProfile -File ./scripts/validate.ps1 -Scan` (docs may contain example tokens) and `-Lint`. Doc-only changes won't affect tests, but per AGENTS.md validation contract I'll run the relevant subset.
- No commit/push unless requested afterward; present a summary of the diffs.

## Non-goals (explicitly out of scope)
- No edits under `image/` (docker image's AGENTS.md/opencode.json are correct as-is and are the image's concern).
- No code, compose, Dockerfile, or workflow changes.
- Won't restore `docs/github-app-webhook-setup.md` (essentials inlined instead).